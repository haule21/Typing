<Window x:Class="TypingApp.Views.OcrOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OcrOverlay" Height="120" Width="380"
        WindowStyle="None" AllowsTransparency="True" Topmost="True" Background="#01000000" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen" Focusable="True">
    <Border Background="#EE000000" CornerRadius="20" BorderBrush="#44FFFFFF" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="txtStatus" Text="Converting IMAGE to TEXT..." 
                       FontSize="18" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" Margin="0,10,0,5">
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="TextBlock.Loaded">
                        <BeginStoryboard x:Name="BlinkStoryboard">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="1.0" To="0.4" Duration="0:0:0.7" 
                                                 AutoReverse="True" RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
            <TextBlock x:Name="txtSubStatus" Text="(Press ESC to Cancel)" 
                       FontSize="12" Foreground="#888888" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
        </StackPanel>
    </Border>
</Window>
